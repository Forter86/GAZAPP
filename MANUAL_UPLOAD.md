# Инструкция по ручной загрузке файлов

## Какие файлы нужно загрузить

### 1. Файл с вопросами (ОБЯЗАТЕЛЬНО!)
📁 **Путь в репозитории:** `public/voprosi.txt`
- Это файл с вопросами для викторины
- Должен быть в папке `public/` в корне проекта

### 2. Компоненты викторины

📁 **Путь:** `src/components/sections/QuizSection.tsx`
- Компонент викторины

📁 **Путь:** `src/components/sections/ApplicationForm.tsx`
- Форма заявки (обновлена для викторины)

📁 **Путь:** `src/utils/parseQuestions.ts`
- Парсер вопросов из файла

📁 **Путь:** `src/App.tsx`
- Главный компонент (обновлен для викторины)

📁 **Путь:** `src/components/atoms/Button.tsx`
- Компонент кнопки (обновлен)

### 3. Конфигурация

📁 **Путь:** `vercel.json`
- Конфигурация для Vercel

📁 **Путь:** `.gitignore`
- Игнорирование больших файлов

## Структура папок в репозитории

```
gazprom-app/arturapps/gazapp/
├── public/
│   └── voprosi.txt          ← ФАЙЛ С ВОПРОСАМИ (важно!)
├── src/
│   ├── App.tsx              ← Обновлен
│   ├── components/
│   │   ├── atoms/
│   │   │   └── Button.tsx   ← Обновлен
│   │   └── sections/
│   │       ├── QuizSection.tsx      ← НОВЫЙ файл
│   │       └── ApplicationForm.tsx  ← Обновлен
│   └── utils/
│       └── parseQuestions.ts        ← НОВЫЙ файл
├── vercel.json              ← Обновлен
└── .gitignore               ← Обновлен
```

## Как загрузить через GitHub веб-интерфейс

### Вариант 1: Создать файлы через интерфейс

1. Зайдите на GitHub в ваш репозиторий
2. Нажмите "Add file" → "Create new file"
3. Введите путь: `public/voprosi.txt`
4. Скопируйте содержимое файла `voprosi.txt`
5. Нажмите "Commit new file"

### Вариант 2: Загрузить файлы

1. Зайдите на GitHub в ваш репозиторий
2. Нажмите "Add file" → "Upload files"
3. Перетащите файлы или выберите их
4. **Важно:** Убедитесь, что путь правильный (например, `public/voprosi.txt`)
5. Нажмите "Commit changes"

## Минимальный набор файлов для работы викторины

Если нужно загрузить только самое необходимое:

### Обязательные файлы:
1. ✅ `public/voprosi.txt` - файл с вопросами
2. ✅ `src/components/sections/QuizSection.tsx` - компонент викторины
3. ✅ `src/utils/parseQuestions.ts` - парсер
4. ✅ `src/App.tsx` - обновленный главный файл
5. ✅ `vercel.json` - конфигурация

### Опциональные (если их еще нет):
6. `src/components/sections/ApplicationForm.tsx` - обновленная форма
7. `src/components/atoms/Button.tsx` - обновленная кнопка
8. `.gitignore` - для игнорирования больших файлов

## Проверка после загрузки

После загрузки файлов на GitHub:

1. Убедитесь, что файл `public/voprosi.txt` виден в репозитории
2. Проверьте, что все компоненты на месте
3. Если проект подключен к Vercel - он автоматически пересоберется
4. Проверьте работу викторины на сайте

## Если файлы уже есть в репозитории

Если некоторые файлы уже есть, просто обновите их:

1. Найдите файл в репозитории
2. Нажмите на карандаш (Edit)
3. Вставьте новое содержимое
4. Нажмите "Commit changes"

## Важно!

- ❌ **НЕ загружайте** файл `public/gzp_video.mp4` (он большой, >25 МБ)
- ✅ **ОБЯЗАТЕЛЬНО загрузите** `public/voprosi.txt` (файл с вопросами)
- ✅ Убедитесь, что путь к файлу правильный: `public/voprosi.txt` (не `src/assets/voprosi.txt`)
